# Privacy

Discord Message Cleanup runs locally in Chrome.

The extension reads the active Discord web session only to scan and delete your own deletable messages in the current chat. It stores temporary progress in `chrome.storage.local` and does not send analytics, logs, tokens, or message content to any external service.

